/** @deprecated Prefer `useContent()` / `getSiteContent()`. Kept for compatibility. */
export {
  defaultContent,
} from "@/lib/content/defaults";

import { defaultContent } from "@/lib/content/defaults";

export const navLinks = defaultContent.navbar.links;
export const services = defaultContent.services.items;
export const projects = defaultContent.projects.items;
export const principles = defaultContent.principles.items;
export const processSteps = defaultContent.process.items;
export const technologies = defaultContent.technology.groups;
export const experienceCards = defaultContent.experience.cards;
export const journey = defaultContent.experience.journey;
export const testimonials = defaultContent.testimonials.items;
export const aboutStats = defaultContent.about.stats;
export const globalRegions = defaultContent.global.regions;
export const teamStats = defaultContent.team.stats;
export const team = defaultContent.team.members;
export const clientLogos = defaultContent.trustBar.logos;
export const projectTypes = defaultContent.contact.projectTypes;
export const budgets = defaultContent.contact.budgets;
export const timelines = defaultContent.contact.timelines;
export const faqs = defaultContent.faq.items;
